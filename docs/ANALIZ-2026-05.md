# Proje Analizi — Tutarsızlıklar, Eksikler ve Öneriler

Tarih: 2026-05-24
Kapsam: `README.md`, `KURULUM.md`, `.github/copilot-instructions.md`, `docs/runbooks/*`, `pyproject.toml`, `requirements.txt`, `mikro_sync/settings.py`, kaynak kod gerçeği.

---

## 1) KRİTİK — Kodu bozan tutarsızlıklar

### 1.1 `mikro_gelen` uygulaması artık yok ama her yerde referans veriliyor

**Gerçek durum:** Workspace kökünde `mikro_gelen/` klasörü mevcut değil. `INSTALLED_APPS` içinde de yok (`mikro_sync/settings.py`).

**Ama hâlâ referans veren yerler:**

| Yer | Sorun |
|---|---|
| [sync_motor/views.py](sync_motor/views.py#L97) | `from mikro_gelen.models import MikroFatura` → **ImportError**. `import_baslat` view'ı çağrılınca patlar. |
| [README.md](README.md#L17) | Mimari diyagramı `mikro_gelen/   (staging — ham veriler)` diyor |
| [README.md](README.md#L31) | "Staging Alanı" özelliği reklam ediliyor |
| [README.md](README.md#L100-L112) | Proje yapısı `templates/mikro_gelen/` ve `mikro_gelen/` klasörünü içeriyor |
| [pyproject.toml](pyproject.toml#L33) | `known-first-party` |
| [pyproject.toml](pyproject.toml#L46) | `[tool.vulture] paths` |
| [docs/runbooks/03-veri-butunlugu-ve-transaction-standardi.md](docs/runbooks/03-veri-butunlugu-ve-transaction-standardi.md) | "fatura durum güncelleme" kuralı |
| [docs/runbooks/04-test-stratejisi-ve-kalite-kapilari.md](docs/runbooks/04-test-stratejisi-ve-kalite-kapilari.md) | `mikro_gelen/tests.py` |
| [docs/runbooks/11-kvkk-ve-kisisel-veri-saklama.md](docs/runbooks/11-kvkk-ve-kisisel-veri-saklama.md) | Tüm runbook bu app etrafında kurulmuş (KVKK staging veri temizleme) |
| [docs/runbooks/13-mikro-sync-entegrasyon-dayanikliligi.md](docs/runbooks/13-mikro-sync-entegrasyon-dayanikliligi.md#L100) | "Outbox/staging" stratejisi |
| [docs/runbooks/14-modulerlik-ve-kod-boyut-standartlari.md](docs/runbooks/14-modulerlik-ve-kod-boyut-standartlari.md#L23) | LOC tablosu |
| [docs/runbooks/16-kod-kalitesi-araclar-ve-git-protokolu.md](docs/runbooks/16-kod-kalitesi-araclar-ve-git-protokolu.md#L48) | Vulture komutu |
| [docs/runbooks/README.md](docs/runbooks/README.md) | Mimari özet |
| `/memories/repo/project-facts.md` | "Apps: ... mikro_gelen (MikroFatura staging)" |
| `.github/copilot-instructions.md` | "Mikro ERP'yi doğrudan sorgulayan ... iki yönlü senkronizasyon köprüsü" — staging silindiğinden artık tek yönlü |

**Karar gerekiyor:** İki seçenekten biri:
- **A)** `mikro_gelen` ve staging tabanlı 5 adımlı pipeline tamamen iptal — tüm referansları temizleyip projeyi "sadece okuma + cari sorgu" olarak yeniden konumlandır.
- **B)** `mikro_gelen` app'i geri getir — staging pipeline'ı yeniden inşa et.

---

### 1.2 Django sürümü çelişkili

| Kaynak | Söylediği |
|---|---|
| [requirements.txt](requirements.txt#L1) | `Django==6.0.5` |
| [README.md](README.md#L4) badge | `Django 5.2 LTS` |
| [README.md](README.md#L64) teknoloji yığını | `Django 5.2 LTS` |
| `.github/copilot-instructions.md` | `Django 5.2` |
| [mikro_sync/settings.py](mikro_sync/settings.py#L1) docstring | Direkt sürüm yazmıyor, "Mikro ERP <-> Muhasebe Bürosu" diyor |
| [docs/runbooks/17-bagimlilik-guncelleme-ve-uyumluluk.md](docs/runbooks/17-bagimlilik-guncelleme-ve-uyumluluk.md) | LTS→LTS atla kuralı |

Django 6.0 LTS değildir (LTS olanlar 4.2 ve 5.2). Kural "LTS olmayan sürüme geçme" diyor ama paket 6.0.5'te. **Karar:** Django 5.2 LTS'e döneceğiz mi yoksa kural değişti mi?

---

### 1.3 `olustur_admin.py` davranışı README'de yanlış

- [README.md](README.md#L52) → "Admin kullanıcı oluşturur (interaktif)"
- [olustur_admin.py](olustur_admin.py#L20-L26) → `ADMIN_SIFRE` tanımlıysa **sessiz**, değilse interaktif
- `.github/copilot-instructions.md` → "sessizce oluşturur" (doğru)
- `.env.example` → `ADMIN_SIFRE=Admin1234!` (yani varsayılan kurulumda sessizdir)

**Düzeltme:** README'de "interaktif" ifadesini "(`.env`'deki `ADMIN_KULLANICI`/`ADMIN_SIFRE` ile sessizce; tanımlı değilse interaktif sorar)" yap.

---

## 2) ORTA — Dokümantasyonun gerçeği yansıtmadığı yerler

### 2.1 README'de eksik uygulamalar

Proje yapısı bölümünde sadece `sync_motor`, `mikro_gelen`, `hesap_yonetimi` var. **Eksik olanlar:**
- `kullanici/` — kullanıcı yönetimi (INSTALLED_APPS'te var)
- `lisans/` — lisans sistemi (özellik tablosunda anılıyor ama yapıda yok)
- `posta/` — SMTP/ekstre gönderim
- `installer/` — kurulum paketi
- `docs/SIFRELER.md`, `docs/more_env/` — referansı yok

### 2.2 `FirmaSecimZorunluMiddleware` belirsiz

[settings.py](mikro_sync/settings.py#L51) MIDDLEWARE listesinde var ama copilot-instructions sadece `LoginZorunluMiddleware`'i listeliyor. Davranışı ne, hangi URL'leri muaf tutuyor?

### 2.3 `FirmaAyar` model alanları doküman dışı

[sync_motor/models.py](sync_motor/models.py#L24-L37) içinde var, copilot-instructions'da yok:
- `mikro_sunucu_vpn` (FortiClient VPN IP)
- `mikro_sunucu_uzak` (RDP içi 127.0.0.1)
- `mikro_api_key`
- `sql_sunucu / sql_veritabani / sql_kullanici / _sql_sifre_sifreli` (SQL bağlantı tipi için)
- `notlar`

Hangi `baglanti_tipi`'nde hangi alan zorunlu — yazılı değil. KURULUM.md sadece `mikro_sunucu` ve `mikro_port` istiyor.

### 2.4 Reklam ile gerçek eşleşmiyor (README özellikler tablosu)

| README diyor | Gerçek |
|---|---|
| "Otomatik Eşleştirme — VKN + SequenceMatcher tabanlı öğrenen cari eşleştirme" | `CariEslestirme` modeli `sync_motor`'da yok (sadece `FirmaAyar`, `ImportLog`) |
| "5 Adımlı Pipeline (Çekme→Eşleştirme→Onay→Aktarım→Doğrulama)" | Staging silinmiş, onay ekranı yok |
| "Staging Alanı — `mikro_gelen/`" | App yok |
| "Onay Ekranı" | Bağlı view/template yok |
| "iki yönlü senkronizasyon" (başlık) | Mikro'ya yazan kod yok; hesap_yonetimi de "sadece okuma" |

---

### 2.5 `hesap_yonetimi/models.py` boş

[hesap_yonetimi/models.py](hesap_yonetimi/models.py) sadece `# Create your models here.` içeriyor. Aktif firma seçimi `request.session["aktif_firma_id"]` ile yapılıyor (copilot-instructions). Bu app'in `migrations/` klasörü gereksiz — veya `tests.py` da kullanılmıyor.

### 2.6 `CONTRIBUTING.md` ve `LICENSE` yok

- [README.md](README.md#L4) → MIT License badge ama `LICENSE` dosyası yok
- [README.md](README.md#L184) → "[CONTRIBUTING.md](CONTRIBUTING.md) okuyun" → dosya yok

### 2.7 "Muhasebe Bürosu" çelişkisi

- [pyproject.toml](pyproject.toml#L4) → "Mikro ERP ↔ Muhasebe Bürosu iki yönlü senkronizasyon köprüsü"
- [mikro_sync/settings.py](mikro_sync/settings.py#L3) docstring → "Mikro ERP <-> Muhasebe Bürosu iki yönlü senkronizasyon servisi"
- `/memories/repo/project-facts.md` → "Muhasebe büro diye bir proje YOK — hepsi mikro_jump_bridge altında birleşti"
- README → "çok firmalı cari hesap yönetimi" (muhasebe bürosu yok)

İki tanım birbirini tutmuyor: ya muhasebe bürosu için bir araç ya da genel cari yönetim aracı.

---

## 3) HAFİF — Düzeltilmesi kolay küçük sorunlar

### 3.1 Bağımlılık sürümleri
- [requirements.txt](requirements.txt#L4) → `requests==2.34.2` (copilot-instructions 2.32 diyor)
- [requirements.txt](requirements.txt#L2) → `whitenoise==6.12.0` (kontrol edilmedi)
- `defusedxml==0.7.1` (copilot 0.7 diyor — ok)

### 3.2 `requirements.txt`'te eksik paketler
[runbook 06](docs/runbooks/06-bagimlilik-ve-tekrarlanabilir-kurulum.md) ve PDF kuralı için `xhtml2pdf`, Excel için `openpyxl` gerekiyor ama [requirements.txt](requirements.txt) içinde yok. Bu özellikler kullanılıyor mu?

### 3.3 `.env.example` ↔ KURULUM.md
- `.env.example` `DEBUG=True` varsayılan
- KURULUM.md `DEBUG=False` öneriyor

Geliştirme/üretim ayrımı net değil. KURULUM.md son kullanıcı için olduğundan `DEBUG=False` doğru, ama `.env.example`'da yorum eklenmeli.

### 3.4 `.env` ile `SIFRELER.md` çakışması
copilot-instructions, `SIFRELER.md` ve `.env`'i birlikte yedeklemekten bahsediyor ama içerik örtüşüyor (ADMIN_SIFRE her ikisinde). Tek doğru kaynak hangisi?

### 3.5 Repo memory güncel değil
`/memories/repo/project-facts.md`:
- `mikro_gelen` ve `CariEslestirme` artık yok
- `kullanici`, `lisans`, `posta` apps listede yok

### 3.6 Runbook 16'da yanlış vulture komutu
[docs/runbooks/16-kod-kalitesi-araclar-ve-git-protokolu.md](docs/runbooks/16-kod-kalitesi-araclar-ve-git-protokolu.md#L48) → `vulture sync_motor mikro_gelen hesap_yonetimi mikro_sync` (yok app + eksik app)
copilot-instructions → `sync_motor hesap_yonetimi lisans posta kullanici mikro_sync` (doğru)

---

## 4) ÖNERİLER — Yeni eklenmesini önerdiklerim

### 4.1 `docs/MIMARI.md`
Tek sayfa güncel mimari diyagramı + middleware sırası + session keys. README'deki kısa diyagramın detaylı sürümü.

### 4.2 `CHANGELOG.md`
v1.0.0 etiketi atılmış (terminal geçmişinde `git tag v1.0.0` görünüyor), ama CHANGELOG yok.

### 4.3 Sağlık kontrolü endpoint
`/saglik/` → DB erişimi + log dosyası yazılabilir mi + (opsiyonel) aktif Mikro bağlantı testi. Müşteride sorun olunca tanı kolaylaşır.

### 4.4 `manage.py kontrol` veya `kontrol.bat` exit code
Şu an `kontrol.bat` hangi adımın patladığında ne döndürüyor belirsiz. CI'a sokmak için exit code disiplini eklenmeli.

### 4.5 İzleme ve uyarı eşiği
[runbook 08](docs/runbooks/08-gozlemlenebilirlik-loglama-ve-denetim-izi.md) loglamayı tanımlıyor ama "şu durumda yöneticiyi maille uyar" gibi alarm eşiği yok. `posta/` app bunu sağlayabilir.

### 4.6 Statik dosya / installer kontrol listesi
`installer/setup.iss` Inno Setup ile paketleme yapıyor; `staticfiles/` runtime'da `collectstatic` ile dolduruluyor (`installer/baslat_kurulu.bat` son commit'inde eklenmiş). Bu süreç bir runbook'a (örn. `19-installer-paketleme.md`) yazılmalı.

### 4.7 Migration disiplini runbook'u
Şu an migration kuralı dağınık. "Hangi durumda migrate, hangi durumda makemigrations + commit zorunlu" gibi tek belge yok.

### 4.8 `docs/more_env/` ne için?
`docs/more_env/config.json`, `copilot-instructions.md` (eski versiyon?), `hesap_bildirim.json` var. Bu klasörün amacı dokümante edilmemiş — silinecek mi, taşınacak mı?

---

## 5) Alınan Kararlar

| # | Karar | Sonuç |
|---|---|---|
| 1 | `mikro_gelen` ve 5 adımlı pipeline | **SİL** — ileride gerekirse yeniden eklenir |
| 2 | Django sürümü | **5.2 LTS'e dön** (runbook 17 kuralı gereği) — _onay bekliyor_ |
| 3 | "İki yönlü senk" + "Muhasebe Bürosu" ifadeleri | **Kaldır** — proje şu an "okuma + cari sorgu" odaklı |
| 4 | `lisans`, `posta`, `kullanici`, `installer` app'leri | README ve copilot-instructions'a **ekle** |
| 5 | `CONTRIBUTING.md` + `LICENSE` (MIT) | **Ekle** |
| 6 | `FirmaAyar` ek alanları (VPN/uzak/api_key/SQL) | Kullanım rehberi **yaz** (KURULUM.md'ye ek bölüm) |
| 7 | `SIFRELER.md` vs `.env` | `.env` **tek otorite**; `SIFRELER.md` sadece müşteri-bazlı kasa notu (içerikleri çakışmasın) — _onay bekliyor_ |
| 8 | `docs/more_env/` klasörü | _Belirsiz_ — kullanıcıya sorulacak |

---

## 6) Faz Planı (uygulama sırası)

Her faz commit edilebilir, bağımsız ilerler. Faz sonunda `kontrol.bat` çalıştırılır.

### FAZ 0 — Mevcut durum koruması
- [ ] `git status` temiz mi kontrol et
- [ ] `db.sqlite3` + `.env` yedeği al (tarihli kopya)

### FAZ 1 — KRİTİK: `mikro_gelen` referanslarını koddan sök
**Hedef:** Uygulama çalıştırılabilir hale gelsin, kırık import kalmasın.

1. [sync_motor/views.py](sync_motor/views.py#L97) → `import_baslat` view'ı:
   - Seçenek A: `from mikro_gelen.models import MikroFatura` satırını ve staging yazma bloklarını çıkar; view'ı "henüz desteklenmiyor" mesajıyla redirect'e indir veya tamamen kaldır (+ URL'sini sil).
   - Seçenek B: `import_baslat`'ı tamamen sil, `ImportLog` modeli yine de geçmiş kayıtlar için kalsın.
   - **Tercih:** B — kullanılmayan view yerine tertemiz çıkış. _Onay bekliyor._
2. [pyproject.toml](pyproject.toml#L33) `known-first-party` listesinden `mikro_gelen` çıkar
3. [pyproject.toml](pyproject.toml#L46) `[tool.vulture] paths` güncelle → `["sync_motor", "hesap_yonetimi", "lisans", "posta", "kullanici", "mikro_sync"]`
4. `kontrol.bat` ve `django check` çalıştır → temiz olmalı
5. Commit: `temizlik: mikro_gelen referansları koddan kaldırıldı`

### FAZ 2 — Dokümantasyon temizliği (`mikro_gelen` ve yanlış reklam)
1. [README.md](README.md):
   - Mimari diyagramından `mikro_gelen` satırını çıkar
   - "Özellikler" tablosundan: Staging Alanı, Onay Ekranı, 5 Adımlı Pipeline, Otomatik Eşleştirme satırlarını kaldır (veya "Yol Haritası" başlığına taşı)
   - "iki yönlü senkronizasyon" → "Mikro ERP cari/hareket sorgulama köprüsü"
   - Proje yapısından `mikro_gelen/` ve `templates/mikro_gelen/` çıkar; `kullanici/`, `lisans/`, `posta/`, `installer/` ekle
   - `olustur_admin.py (interaktif)` → `(`.env`'den sessiz; tanımlı değilse interaktif)`
2. [.github/copilot-instructions.md](.github/copilot-instructions.md):
   - "iki yönlü senkronizasyon" → tek yönlü okuma vurgusu
   - `mikro_gelen` ve `CariEslestirme` referansları temizle
   - `FirmaSecimZorunluMiddleware` ekle (davranışını yaz)
3. Commit: `docs: mikro_gelen referansları ve yanlış reklam metni temizlendi`

### FAZ 3 — Runbook senkronu
1. `docs/runbooks/03, 04, 11, 13, 14, 16, README` — `mikro_gelen`'i çıkar veya "geçmiş tasarım notu" başlığına taşı
2. Runbook 11 (KVKK) → kapsamı `ImportLog` ve oturum verilerine indir; staging veri temizleme kuralını sil veya "ileride app eklendiğinde" notuyla bırak
3. Runbook 16 vulture komutu → güncel app listesiyle değiştir
4. Commit: `runbook: app listesi güncellendi, mikro_gelen kuralları arşivlendi`

### FAZ 4 — Bağımlılıklar ve sürüm uyumu
1. [requirements.txt](requirements.txt):
   - `Django==6.0.5` → `Django==5.2.x` (en güncel 5.2 patch) — _onay bekliyor_
   - `requests==2.34.2` doğrula (kurulu sürüm neyse) → copilot-instructions'ı eşle
   - Kullanılıyorsa `xhtml2pdf`, `openpyxl` ekle; kullanılmıyorsa runbook 18'i sadeleştir
2. `pip install -r requirements.txt` + `kontrol.bat` çalıştır
3. Commit: `bagimlilik: Django 5.2 LTS'e indirildi, requirements senkronu`

### FAZ 5 — Eksik dosyalar
1. `LICENSE` (MIT, telif: Ali Çetin) ekle
2. `CONTRIBUTING.md` — kısa: branch akışı, commit formatı, `kontrol.bat` zorunluluğu
3. `CHANGELOG.md` — v1.0.0 girişi
4. Commit: `docs: LICENSE, CONTRIBUTING, CHANGELOG eklendi`

### FAZ 6 — `FirmaAyar` ve kurulum rehberi tamamlama
1. [KURULUM.md](KURULUM.md) Mikro ERP Bağlantısı bölümüne:
   - `baglanti_tipi` seçim açıklaması (api / sql / manuel)
   - VPN / Uzak Masaüstü / LAN IP'sinin hangi durumda doldurulacağı
   - `mikro_api_key` ne zaman gerekir
   - SQL bağlantı için zorunlu alanlar
2. Commit: `kurulum: FirmaAyar tüm bağlantı seçenekleri dokumante edildi`

### FAZ 7 — Şifre kasası tek otorite
1. `SIFRELER.md`'yi sadece "müşteri kurulum bilgileri" (Mikro IP/şifre, müşteri admin bilgileri) için bırak
2. Django admin / SECRET_KEY → sadece `.env` otorite
3. copilot-instructions'da çakışan ifadeyi sadeleştir
4. Commit: `dokuman: .env tek otorite, SIFRELER.md müşteri kasası olarak ayrıldı`

### FAZ 8 — Repo memory ve `docs/more_env/`
1. `/memories/repo/project-facts.md` güncelle: `mikro_gelen`/`CariEslestirme` çıkar, tüm apps ekle
2. `docs/more_env/` → kullanıcının kararına göre sil veya `docs/arsiv/more_env/` altına taşı

### FAZ 9 (OPSİYONEL) — Önerilen yeni belgeler
- `docs/MIMARI.md` — middleware sırası + session keys + akış
- `docs/runbooks/19-installer-paketleme.md`
- `docs/runbooks/20-migration-disiplini.md`
- `/saglik/` endpoint + `kontrol.bat` exit code disiplini

---

## 7) Devam etmeden önce 3 hızlı onay

1. **FAZ 1 → `import_baslat` view'ı:** tamamen silmeyi tercih ediyor musun (URL + template dahil), yoksa "yakında" mesajına indirelim mi?
2. **FAZ 4 → Django:** 5.2 LTS'e geri dönüş onaylıyor musun? (db.sqlite3 yedeği alınacak, migration'lar zaten Django 5.2 uyumlu olmalı.)
3. **FAZ 8 → `docs/more_env/`:** sil, arşive taşı, yoksa şimdilik dokunma?

Cevap verince FAZ 0 + FAZ 1'den başlıyorum.
